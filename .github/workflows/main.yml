name: main-workflow2
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "build started"
      - run: echo "build ended"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: eho "test started"
      - run: echo "test ended"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploy started"
      - run: echo "deploy ended"
    needs: test
      
